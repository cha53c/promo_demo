<!--TODO can this be generalised for all forms?-->
<% if model.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(model.errors.count, "error") %> prohibited
        this from being saved:</h3>
      <ul>
        <% model.errors.full_messages.each do |msg| %>
            <li class="alert alert-danger"><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>