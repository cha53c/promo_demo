<h1>Home#index</h1>
<%= render 'carousel' %>

<div class="promo-block">

  <% @promotions.each do |promotion| %>
      <%= render 'promotions/card', :promotion => promotion, :client => find_client(promotion.client_id) %>
  <% end %>
</div>
