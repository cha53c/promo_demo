.container
  %h1
    All Users
    = @users.length

  .table-responsive
    %table.table.table-striped
      %thead
        %tr
          %th Username
          %th Role
          %th Login Count
          %th Last Login
          %th Action
      %tbody
        - @users.each do |user|
          %tr
            %td
              = user.email
            %td
              TBC
            %td
              n/a
            %td
              hh:mm:ss dd/MM/yyyy
            %td
              -# TODO this should not be User.new it should be the actual user
              - if policy(User.new).destroy?
                = link_to 'Delete', 'users/destroy'
              = link_to 'Show', user

