.container
  %h1
    All Users
    = @users.length

  .table-responsive
    %table.table.table-striped
      %thead
        %tr
          %th Username
          %th Role
          %th Login Count
          %th Last Login
          %th Action
      %tbody
        - @users.each do |user|
          %tr
            %td
              = user.email
            %td
              TBC
            %td
              n/a
            %td
              hh:mm:ss dd/MM/yyyy
            %td
              = link_to "Delete", 'users/destroy'
              = link_to "Edit", 'users/edit'


              -#%ul
              -#  - @users.each do |user|
              -#    %li
              -#      %span
              -#        = user.email
              -#        = link_to "Delete", 'users/destroy'