.row
  - promotions.each do |promotion|
    .col-sx-12.col-sm-4.col-md-3
      -#= render 'promotions/card2', promotion: promotion, client: find_client(promotion.client_id)
      = render 'promotions/card3', promotion: promotion, client: find_client(promotion.client_id)